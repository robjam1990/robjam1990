using static System.Console;

namespace NPC
{
    public class NPC : Character
    {


        public NPC(string name, string occupation, int age) : base("NPC", 0, 1.0f, 1.0f, true)
        {
        }
    }
}
